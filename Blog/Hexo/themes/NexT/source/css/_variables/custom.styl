
/******* 如何更改字体？ *******/

// 标题，修改成你期望的字体族
//$font-family-headings = Georgia, sans

// 修改成你期望的字体族
//$font-family-base = "Microsoft YaHei", Verdana, sans-serif

// 代码字体
//$code-font-family = "Input Mono", "PT Mono", Consolas, Monaco, Menlo, monospace

// 正文字体的大小
$font-size-base           = 16px
$font-size-base           = unit(hexo-config('font.global.size'), px) if hexo-config('font.global.size') is a 'unit'
$font-size-small          = $font-size-base - 2px
$font-size-smaller        = $font-size-base - 4px
//$font-size-large          = $font-size-base + 2px //默认大屏幕字体
$font-size-large          = $font-size-base + 0px
$font-size-larger         = $font-size-base + 4px

// 代码字体的大小
//$code-font-size = 13px

/******* 如何更改内容区域的宽度？ *******/

// 修改成你期望的宽度
$content-desktop = 700px

// 当视窗超过 1600px 后的宽度
$content-desktop-large = 1000px